{
  "name": "unser-kochbuch",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/pruefe-rezepte.mjs && astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "astro check",
    "pruefen": "node scripts/pruefe-rezepte.mjs"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.112.0",
    "pagefind": "^1.3.0"
  }
}
